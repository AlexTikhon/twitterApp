// Minimal brand mark reused in the main navigation.
import React from 'react';

import './Logo.css';

// Renders the text logo used by the main navigation.
const logo = () => <h1 className="logo">MessageNode</h1>;

export default logo;
